# tyzo

This is the monorepo for tyzo, which contains:

- The page editor. This is a standalone page builder component for react.
- The tyzo core package, which contains the app to edit your react projects, using the page builder in turn.
- (Soon) Integration packages for next.js etc.
